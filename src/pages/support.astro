---
import BaseLayout from '../layouts/BaseLayout.astro';
import { useTranslations, getLocalizedPath } from '../i18n/utils';
import { getContent } from '../i18n/content';

const lang = 'en' as const;
const t = useTranslations(lang);
const c = getContent(lang);

const topics = c.supportTopics;
const faqs = c.supportFaqs;

const structuredData = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  name: 'Lade Stack Support',
  url: 'https://ladestack.in/support',
  description: 'Support center and FAQ for Lade Stack platform.',
  mainEntity: faqs.map((f) => ({ '@type': 'Question', name: f.q, acceptedAnswer: { '@type': 'Answer', text: f.a } }))
};
---
<BaseLayout
  lang={lang}
  path="/support"
  title="Support Center — FAQs, Troubleshooting & Help | Lade Stack"
  description="Lade Stack support center: fix loading issues, file-tool tips, CodeEnhance AI help, FAQs and 24-hour contact. Free forever tools."
  structuredData={structuredData}
>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-14 md:py-20">
    <h1 class="font-serif text-4xl md:text-6xl font-bold">Support Center</h1>
    <p class="mt-3 text-foreground/65 max-w-2xl md:text-lg">{t('support.subtitle')}</p>

    <div class="mt-8 grid sm:grid-cols-2 lg:grid-cols-4 gap-4">
      {topics.map((topic) => (
        <div class="rounded-2xl border border-border bg-card/60 p-5">
          <h2 class="font-semibold">{topic.title}</h2>
          <p class="mt-2 text-sm text-foreground/65">{topic.text}</p>
        </div>
      ))}
    </div>

    <h2 class="font-serif text-2xl md:text-3xl font-bold mt-12">Frequently asked questions</h2>
    <div class="mt-5 grid lg:grid-cols-2 gap-3 items-start">
      {faqs.map((f) => (
        <details class="rounded-2xl border border-border bg-card/60 px-5 py-4 open:border-primary/40">
          <summary class="cursor-pointer font-medium list-none flex items-center justify-between gap-3">
            {f.q}
            <span aria-hidden="true" class="text-primary text-xl leading-none">+</span>
          </summary>
          <p class="mt-3 text-sm text-foreground/65 leading-relaxed">{f.a}</p>
        </details>
      ))}
    </div>

    <div class="mt-10 rounded-3xl bg-brand text-white p-8 text-center">
      <h2 class="font-serif text-2xl font-bold">{t('support.stillStuck')}</h2>
      <p class="mt-2 text-white/85">{t('support.respondNote')}</p>
      <a href={getLocalizedPath('/contact', lang)} class="mt-5 inline-flex h-11 px-7 items-center justify-center rounded-full bg-white text-ink font-semibold">{t('support.contactCta')}</a>
    </div>
  </div>
</BaseLayout>
